spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory-tracker
    username: inventory-tracker
    password: inventory-tracker
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
server:
  port: 8084
inventorytracker:
  baseUrl: http://192.168.178.115:8084
